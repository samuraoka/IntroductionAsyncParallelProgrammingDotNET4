namespace Fenestra
{
    /// <summary>Controls the spin direction.</summary>
    public enum BizzySpinnerDirection { 
        
        /// <summary>Spin is clock wise (left to right)</summary>
        Clockwise,

        /// <summary>Spin is counter clock wise (right to left)</summary>
        CounterClockwise 
    };
}